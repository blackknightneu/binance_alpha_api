# Copy file này thành .env và cập nhật MongoDB URI
MONGODB_URI=mongodb://localhost:27017/binance_alpha

# Hoặc sử dụng MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/binance_alpha

# Port (optional, mặc định 3000)
PORT=3000
